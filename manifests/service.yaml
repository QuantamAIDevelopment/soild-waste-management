apiVersion: v1
kind: Service
metadata:
  name: swm-route-optimizer
  namespace: default
spec:
  selector:
    app: swm-route-optimizer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
