apiVersion: v1
kind: Service
metadata:
    name: quantamaidevelopmentsoildwastemanagement
spec:
    type: LoadBalancer
    ports:
    - port: 8000 
    selector:
        app: quantamaidevelopmentsoildwastemanagement