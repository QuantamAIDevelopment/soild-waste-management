apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swm-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: swm.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: swm-service
            port:
              number: 80
